#!/bin/bash

. /usr/local/lib/cephos/cephos.sh.lib

read -r -d '' help_text <<EOF
Usage: $0 <node_host>
Example: $0 ceph-second.domain.local
EOF

set -euo pipefail

if [[ $# -lt 3 ]]; then
  help
  exit 1
fi

node="${1}"

ssh-copy-id cephos@${node}
